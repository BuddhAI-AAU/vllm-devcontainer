# --- Core API ---
fastapi==0.128.0
uvicorn==0.40.0
starlette==0.50.0
psycopg2-binary==2.9.12

# --- Pydantic ---
pydantic==2.12.5
pydantic-core==2.41.5
pydantic-settings==2.12.0
typing_extensions==4.15.0

# --- LangChain / LangGraph (snapshot versions) ---
langchain==1.2.15
langchain-core==1.3.2
langchain-protocol==0.0.12
langgraph==1.1.10
langgraph-checkpoint==4.0.3
langgraph-prebuilt==1.0.12
langgraph-sdk==0.3.13
langsmith==0.7.37

# --- vLLM (snapshot versions) ---
vllm==0.20.0
vllm-omni==0.20.0

# --- Transformers / Torch ---
transformers==4.57.1
torch==2.11.0
torchaudio==2.11.0
torchvision==0.26.0

# --- Whisper STT (snapshot version) ---
openai-whisper==20250625

# --- Audio processing ---
soundfile==0.13.1   # snapshot version

# --- HTTP clients ---
httpx==0.28.1       # snapshot version
requests==2.32.5    # snapshot version

# --- Serialization / performance ---
orjson==3.11.8      # snapshot version
ormsgpack==1.12.2   # snapshot version
msgpack==1.1.2      # snapshot version
xxhash==3.7.0       # snapshot version
zstandard==0.25.0   # snapshot version

# --- Utilities ---
tqdm==4.67.1         # snapshot version
filelock==3.20.3     # snapshot version
jinja2==3.1.6        # snapshot version
